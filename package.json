{
  "name": "oxy-chat-relay",
  "version": "1.0.0",
  "description": "Global cross-server chat relay for the Oxy hub (WebSocket + HTTP fallback, moderated).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.0"
  }
}
